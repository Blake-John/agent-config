# 循环状态命令

检查活跃循环的状态、进度和失败信号。

## 用法

`/loop-status [--watch]`

## 报告内容

- 活跃循环模式
- 当前阶段和最后成功检查点
- 失败的检查（如有）
- 预计时间/成本偏差
- 建议干预（继续/暂停/停止）

## 观察模式

使用 `--watch` 时，定期刷新状态并显示状态变更。

## 参数

$ARGUMENTS：
- `--watch` 可选
